<h1>Personer:</h1>


<%= link_to 'Lägg till ny person', new_person_path %>

 
<table>
  <tr>
    <th>Namn</th>
    <th>Personnummer</th>
    <th>Kortnummer</th>
  </tr>
 
  <% @people.each do |person| %>
    <tr>
      <td><%= person.name %></td>
      <td><%= person.personnbr %></td>
      <td><%= person.cardnbr %></td>
      <td><%= link_to 'Visa', person_path(person) %></td>
      <td><%= link_to 'Ändra', edit_person_path(person) %></td>
    </tr>
  <% end %>
</table>